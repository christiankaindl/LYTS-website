---
title: Box
description: Lowest common denominator
---

import { Box } from '@christiankaindl/lyts'

Box is a generic component, providing useful features such as `bleed`, `asChild`, `xAlign` and `yAlign`. All layout components render a Box under-the-hood and inherit its props.
