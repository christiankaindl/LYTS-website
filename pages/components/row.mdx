import { Stack, Row, Clamp, Columns, Grid, Split } from '@christiankaindl/lyts'
import SplitRow from '@/examples/SplitRow'
import CenterWrappedRow from '@/examples/CenterWrappedRow'
import RowColumns from '@/examples/RowColumns'
import RowColumnsCustomRatios from '@/examples/RowColumnsCustomRatios'

<Row>
  <RowIcon />
  <Stack gap={0.25}>
    # Row
    A horizontal stack
  </Stack>
</Row>

<Row>
  <button>Save</button>
  <button>Delete</button>
  <button>Share</button>
</Row>

Row composes nicely with Split to create split layouts:

<SplitRow />

With the `wrap` prop you can also achieve common "pill" style inline metadata/tags/etc:

<CenterWrappedRow />

The `expandChildren` prop let's you customize the columns `flex-grow` values:

<RowColumns />

Or with custom ratios:

<RowColumnsCustomRatios />
