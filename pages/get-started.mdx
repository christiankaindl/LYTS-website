---
title: Get started
description: Add LYTS to your project in < 1 minute.
---

import Link from 'next/link'

export { default } from 'components/SidebarLayout/SidebarLayout'

Build any kind of layout quickly and confidently. LYTS privdes a set of layout primitives which you can use directly, or build on top of. All components work out-of-the-box with zero configuration and sensible defaults.

## Installation

```sh
npm install @christiankaindl/lyts
```

## Usage


Import the library's CSS styles into your application. For example in a Next.js app, import in `_app.js`:

```jsx
/** _app.js */
import '@christiankaindl/lyts/style.css'

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />
}

export default MyApp
```

That's the whole setup! Now import one of the available components--Stack, Row, Clamp, Columns, Grid--and use them to get nice and consistent layouts.


## Examples

Compose layout components until you achieve your desired layout. The following \<CenterCard> component creates a card with a max-width of 750px and centers it:

```tsx
const CenterCard: FunctionComponent = function () {
  return (
    // A card with a max-width of 750px and centers it
    <Clamp clamp='750px'>
      <Stack gap={0.75} className='card'>
        <img src='' />
        <h3>Card title</h3>
        <Row wrap gap={0.5}>
          <InfoIcon />
          <p>Info: This is a card</p>
        </Row>
        <Box xAlign='end'>Aligned to the right</Box>
      </Stack>
    </Clamp>
  )
}
```

Check out the <Link href='/examples'>Examples</Link> page for a comprehensive collection of layouts and how to build them with LYTS.

## Support & help

If you get stuck, open a discussion on GitHub or reach out to @christiankaindl on Twitter.

In case of bugs, open an issue on GitHub.
