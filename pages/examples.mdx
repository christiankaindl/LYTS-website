---
title: Examples
description: Copy-pastable code snippets
---

import { Stack, Row, Clamp, Columns, Grid, Split } from '@christiankaindl/lyts'
export { default } from '@/components/SidebarLayout/SidebarLayout'

### Align a single element (Box)

### Control center (Grid)
Using grid to create a compact controls interface

### Blog posts (Grid)

### Boxed lists (Row)
Boxed lists are becoming more and more common in UI design.

### Horizontal Navbar (wrapped + scrolled) (Row, Split)
Common split pattern, wrapped into a two-line layout on mobile.

### Stepper/Checkout (Row)
